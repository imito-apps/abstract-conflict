package imito.ac.entities.cards.sets.main.basic

import imito.ac.entities.cards.sets.main.base.*

class Domination(
    isShown: Boolean = false,
) : DominationBase(Id, 9, isShown) {
    companion object {
        const val Id = 9
    }
}
